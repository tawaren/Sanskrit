signed(A) transaction{
   a = lit 1:$u8;
   b = pack SomeTypes.X[$u8]#Ctr(a);
   d = call IndexOps.ModIndex();
   (r, s) = call SomeTypes.GenIndexedAt[SomeTypes.X[$u8]](d,b);
   store s;
};

signed(A) transaction{
   a = call IndexOps.ModIndex();
   r = call SomeFunctions.GenRef(a);
   b = &load r;
};

signed(A) transaction{
   a = call IndexOps.ModIndex();
   r = call SomeFunctions.GenRef(a);
   b = load r;
   store b;
};

signed(A) transaction{
   a = lit 2:$u8;
   b = pack SomeTypes.X[$u8]#Ctr(a);
   d = call IndexOps.ModIndex();
   (r, s) = call SomeTypes.GenIndexedAt[SomeTypes.X[$u8]](d,b);
   store s;
};

signed(A) transaction{
   a = call IndexOps.ModIndex();
   r = call SomeFunctions.GenRef(a);
   b = &load r;
};