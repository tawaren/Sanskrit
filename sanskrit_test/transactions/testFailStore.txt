signed(A) transaction{
   a = lit 1:$u8;
   b = pack SomeTypes.NoDrop[$u8]#Ctr(a);
   b2 = copy b;
   d = call IndexOps.ModIndex();
   d2 = copy d;
   (r, s) = call SomeTypes.GenIndexedAt[SomeTypes.NoDrop[$u8]](d,b);
   (r2, s2) = call SomeTypes.GenIndexedAt[SomeTypes.NoDrop[$u8]](d2,b2);
   store s;
   store s2;
};
