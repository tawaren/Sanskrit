module TestFailStealLater3 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](consume a:.X[A]):(c:A, b:A){
       b = unpack a:.X[A];
       c = &b;
       return (c, b);
    };
}