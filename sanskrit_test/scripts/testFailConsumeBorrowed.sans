module TestFailConsumeBorrowed {

   type <Drop, Embed> X[<Embed> T]{
        Ctr(T);
   };

   private fun Test[<Embed,Drop> A](a:A):(){
       b = &a;
       c = pack .X[A]#Ctr(b);
       return;
   };
}