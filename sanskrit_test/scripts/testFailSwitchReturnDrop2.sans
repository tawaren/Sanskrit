module TestFailSwitchReturnDrop2 {
   type Option[T] {
       Some(T);
       None;
   };

   fun TestFunApply[<Drop, Copy, Value, Unbound> A](consume a:.Option[A], a2:A):(b:A) {
       b = switch a:.Option[A] {
          Some(a) => {
            discard a;
            return a;
          };
          None => {
            return;
          };
       };
       return b;
   };
}