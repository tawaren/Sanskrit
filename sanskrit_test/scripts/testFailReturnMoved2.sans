module TestFailReturnMoved2 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](consume a:A):(b:.X[A]){
       b = pack .X[A]#Ctr(a);
       drop b;
       return b;
    };
}