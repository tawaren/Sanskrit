module TestFailConsumeBorrowed3 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](a:.X[A]):(){
       b = &unpack a:.X[A];
       c = pack .X[A]#Ctr(b);
       return;
    };
}