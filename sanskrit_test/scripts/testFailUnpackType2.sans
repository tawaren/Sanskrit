module TestFailUnpackType2 {

    type X[<Embed> A] {
        X(A);
    };

    type Y[<Embed> A] {
        Y(A);
    };

    private fun Test[<Embed> A](a:.X[A]):(borrowed(a) b:A){
       b = &unpack a:.Y[A];
       return b;
    };
}