module TestSucAdtImport {
    type Importer[<Embed> A,<Embed> B] {
        Phantom(TestSucAdt.Phantom);
        Wrapper(TestSucAdt.Wrapper[A]);
        Enum(TestSucAdt.Enum);
        Option(TestSucAdt.Option[A]);
        Alternative(TestSucAdt.Alternative[A,B]);
        Struct(TestSucAdt.Struct);
    };
}