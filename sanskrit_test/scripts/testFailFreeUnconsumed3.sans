module TestFailFreeUnconsumed3 {

    type <Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed> A](consume a:.X[A]):(){
       b = unpack a:.X[A];
       return;
    };
}