module TestFailFreeUnconsumed3 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](consume a:.X[A]):(){
       b = unpack a:.X[A];
       return;
    };
}