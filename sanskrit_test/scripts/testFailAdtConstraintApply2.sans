module TestFailAdtConstraintApply2 {
    type Inner[<Drop> A] {
        Inner;
    };

    type <Drop> Inner2[T] {
        Inner2;
    };

    type Wrapper[A] {
        Ctr(.Inner[.Inner2[A]]);
    };
}