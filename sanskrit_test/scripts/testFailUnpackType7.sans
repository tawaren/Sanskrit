module TestFailUnpackType7 {

    type X[A] {
        X1(A);
        X2(A);
    };

    private fun Test[<Value, Unbound> A](a:.X[A]):(b:A){
       b = unpack a:.X[A];
       return b;
    };
}