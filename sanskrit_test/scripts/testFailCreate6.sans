module TestFailCreate6 {
    type <Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed> A](a:.X[A]):(c:.X[A]){
       b = &unpack a:.X[A];
       c = pack .X[A]#Ctr(b);
       return c;
    };
}