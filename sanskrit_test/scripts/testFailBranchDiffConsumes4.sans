module TestFailBranchDiffConsumes4 {
    err X;

    private fun Test[<Drop> A,<Drop> B](consume a:A, consume b:B):(){
        () = try {
            drop a;
            return;
        } catch {
            .X => {
                drop b;
                drop a;
                return;
            };
        };

        return;
    };
}