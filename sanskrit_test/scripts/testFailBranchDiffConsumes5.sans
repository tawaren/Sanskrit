module TestFailBranchDiffConsumes5 {
    err X;

    private fun Test[<Drop> A,<Drop> B](consume a:A, consume b:B):(){
        () = try {
            discard a;
            discard b;
            return;
        } catch {
            .X => {
                discard a;
                return;
            };
        };

        return;
    };
}