module TestFailStealDropped2 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](consume a:A):(borrowed(a) c:.X[A]){
       b = pack .X[A]#Ctr(a);
       c = &b;
       return c;
    };
}