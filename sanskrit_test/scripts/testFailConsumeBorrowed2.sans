module TestFailConsumeBorrowed2 {

    type <Drop, Embed> X[<Embed> A]{
        Ctr(A);
    };

    private fun Test[<Embed, Drop> A](a:A):(){
       b = &pack .X[A]#Ctr(a);
       c = pack .X[X[A]]#Ctr(b);
       return;
    };
}