module TestFailSwitchDrop2 {
   type Option[<Embed> T] {
       Some(T);
       None;
   };

   fun TestFunApply[<Embed, Drop> A](consume a:.Option[A], a2:A):() {
       b2 = a2;
       () = switch a:.Option[A] {
          Some(a) => {
            discard(a,b2);
          };
          None => {
            return;
          };
       };
       return;
   };
}