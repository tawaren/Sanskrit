module TestFailSwitchType {

    type X[A] {
        X(A);
    };


    private fun Test[<Value, Unbound> A](a:.X[A]):(b:A){
       b = switch a:.X[A] {
            X(a) => {
                return a;
            };
       };
       return b;
    };
}