module TestFailImagePack3 {

    type <Embed> Wrapper[<Embed> X] {
        Ctr(X);
    };

    private fun Test[<Embed> A](consume a:%A):(b:%.Wrapper[A]){
       b = pack .Wrapper[%A]#Ctr(a);
       return b;
    };
}