module TestFailConsumeMoved2 {

    type <Drop, Value, Unbound>  X[A]{
        Ctr(A);
    };

    private fun Test[<Drop, Value, Unbound>  A](consume a:A):(b:.X[A]){
       b = pack .X[A]#Ctr(a);
       discard a;
       return b;
    };
}