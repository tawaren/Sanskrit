module TestFailPackType {

    type <Embed> X[<Embed> A]{
            Ctr(A);
    };

    private fun Test[<Embed> A](consume a:A):(b:TestSucAdt.Wrapper[A]){
       b = pack .X[IntU8.U8]#Ctr(a);
       return b;
    };
}