module TestFailImagePack {

    type <Value, Unbound> Wrapper[X] {
        Ctr(X);
    };

    private fun Test[<Value, Unbound> A](consume a:%A):(b:.Wrapper[A]){
       b = pack %.Wrapper[A]#Ctr(a);
       return b;
    };
}