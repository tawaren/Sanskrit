module TestFailSwitchType2 {

    type X[<Embed> A] {
        X(A);
    };


    private fun Test[<Embed> A](a:.X[A]):(borrowed(a) b:A){
       b = &switch a:.X[A] {
            X(a) => {
                return a;
            };
       };
       return b;
    };
}