module TestFailImagePack2 {

    type Wrapper[X] {
        Ctr(X);
    };

    private fun Test[A](consume a:A):(b:%.Wrapper[A]){
       b = pack %.Wrapper[A]#Ctr(a);
       return b;
    };
}