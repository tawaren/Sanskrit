module TestFailCapUnpack {

    type <Embed> X[<Embed> A] {
        X(A);
    };


    private fun Test[<Embed> A](a:TestSucAdt.Wrapper2[A]):(b:A){
       b = unpack a:TestSucAdt.Wrapper2[A];
       return b;
    };
}