module TestFailBranchDiffConsumes {
    err X;

    private fun Test[<Drop> A,<Drop> B](consume a:A, consume b:B):(){
        () = try {
            discard a;
            return;
        } catch {
            .X => {
                discard b;
                return;
            };
        };

        return;
    };
}