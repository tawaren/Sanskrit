module TestFailTryReturn {
   err X;

   fun TestFunApply[A](consume a:A):(a:A) {
       b = a;
       d = try {
          return b;
       } catch {
         .X => {
           c = b;
           return c;
         };
       };
       return d;
   };
}