module TestFailBranchDiffConsumes2 {
    err X;

    private fun Test[<Drop> A,<Drop> B](consume a:A, consume b:B):(){
        () = try {
            return;
        } catch {
            .X => {
                drop b;
                return;
            };
        };

        return;
    };
}