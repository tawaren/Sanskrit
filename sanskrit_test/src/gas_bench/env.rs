#[cfg(test)]

use super::*;
/*
#[cfg(test)]
mod full_hash {
    use super::*;
    const OP:[Operand;1] = [Operand::FullHash;1];

    #[bench]
    fn bench(b: &mut Bencher) {
        execute_native(b, |a|vec![vec![]], &OP, true);
    }

}

#[cfg(test)]
mod txt_hash {
    use super::*;
    const OP:[Operand;1] = [Operand::TxTHash;1];

    #[bench]
    fn bench(b: &mut Bencher) {
        execute_native(b, |a|vec![vec![]], &OP, true);
    }

}

#[cfg(test)]
mod code_hash {
    use super::*;
    const OP:[Operand;1] = [Operand::CodeHash;1];

    #[bench]
    fn bench(b: &mut Bencher) {
        execute_native(b, |a|vec![vec![]], &OP, true);
    }

}

#[cfg(test)]
mod block_no {
    use super::*;
    const OP:[Operand;1] = [Operand::BlockNo;1];

    #[bench]
    fn bench(b: &mut Bencher) {
        execute_native(b, |a|vec![vec![]], &OP, true);
    }

}


*/